# DevOps with Docker - Exercise 1.15

This is a simple static web application served using Python's `http.server`.

## How to run

```bash
docker run -p 8000:8000 pr0g3t/devops-with-docker
```

Access the application at http://localhost:8000

Testing